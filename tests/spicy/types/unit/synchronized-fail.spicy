# @TEST-DOC: Checks validation of &synchronized attributes.
#
# @TEST-EXEC-FAIL: spicyc -d -p %INPUT >>output 2>&1
# @TEST-EXEC: btest-diff output

module test;

type X = unit {
    a : uint8 &synchronized;
};

type Y = unit {
    x: X &synchronized;
};
