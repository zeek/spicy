# @TEST-EXEC-FAIL: spicyc -j -d %INPUT >> output 2>&1
# @TEST-EXEC: btest-diff output

module Test;

public type X = unit {
    a: uint8 { print $$; }
    on %done { print $$; }
    : void { print $$; }
    : skip &size=1 { print $$; }
};

on Test::X::%done { print $$; }
