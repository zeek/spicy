# @_TEST-EXEC:  printf "AABBB\005" | spicy-driver %INPUT >output
# @TEST-EXEC:  printf "\001AAA\005" | spicy-driver %INPUT -D cfg >>output
# @TEST-EXEC:  btest-diff output

module HTTP;

type Item = unit {
    x: uint8(1);
};

public type Test = unit {
    a: Item[];
    b: b"BBB";
};
