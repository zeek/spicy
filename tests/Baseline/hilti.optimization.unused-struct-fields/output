### BTest baseline data generated by btest-diff. Do not edit. Use "btest -U/-u" to update. Requires BTest >= 0.63.
[$not_accessed_at_all=(optimized out), $not_accessed_at_all_either_but_always_emit="", $only_reads=(optimized out), $only_writes=(optimized out)]
[$not_accessed_at_all_but_type_is_always_emit=""]
----
[debug/optimizer-passes]     Test::X::not_accessed_at_all  #reads=0 #writes=0 #unsets=0
[debug/optimizer-passes]     Test::X::only_reads  #reads=1 #writes=0 #unsets=0
[debug/optimizer-passes]     Test::X::only_writes  #reads=0 #writes=1 #unsets=0
----
module Test {

import hilti;

type X = struct {
    string not_accessed_at_all &no-emit="optimized";
    string not_accessed_at_all_either_but_always_emit &always-emit;
    string only_reads &no-emit="optimized";
    string only_writes &no-emit="optimized";
};
type AlwaysEmit = struct {
    string not_accessed_at_all_but_type_is_always_emit;
} &always-emit;

global AlwaysEmit y;
global X x;

assert default<string>() == "";
hilti::print(x, True);
hilti::print(y, True);

}
